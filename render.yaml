services:
  - type: web
    name: trading-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run_services:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: StocksDB
          property: connectionString
